package com.nigelhenshaw.kotlincore

import android.app.Activity
import android.os.Bundle

/**
 * This exercise will demonstrate the benefit of the KTX core module.
 * The Toast method will be used as an example where its usage
 * will be compared against the KTX core toast() method.
 */
class MainActivity : Activity() {

    /**
     * First implement the standard Android Toast() call.
     * Then replace that with the KTX Core toast() method.
     */
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)

    }
}
